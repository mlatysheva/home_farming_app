import React from "react";

export const NotFoundPage = () => {
  return (
    <div className="main home-page">
      <h1>We looked everywhere but did not find the requested page</h1>
      <h2>It must be somewhere near the remote control</h2>    
    </div>
  )
};